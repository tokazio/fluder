apply plugin: 'maven-publish'

publishing {
    repositories {
        maven {
            url privateRepoUrl
            authentication(userName: privateRepoUser, password: privateRepoPass)
        }
    }
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
