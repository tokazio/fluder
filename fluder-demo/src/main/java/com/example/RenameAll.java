package com.example;

import fr.tokazio.fluder.annotations.Buildable;

@Buildable(builderName = "renamer", creatorName = "$gen", intermediatePrefix = "chain", buildMethodName = "Go", instanceMethodName = "CreateNew")
public class RenameAll {

    String test;
}
