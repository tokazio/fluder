apply plugin: 'maven-publish'

dependencies {
    implementation  project (':fluder-annotations')
}

publishing {
    repositories {
        maven {
            credentials {
                username System.getenv('PRIVATEREPOUSER')
                password System.getenv('PRIVATEREPOPASS')
            }
            url = System.getenv('PRIVATEREPOURL')
        }
        maven {
            credentials {
                username = 'tokazio'
                password = '8676f8cda271cb02b21a311c4f358bc538a7bd3f'
            }
            url = 'https://maven.pkg.github.com/tokazio'
    }
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
